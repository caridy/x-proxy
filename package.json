{
  "name": "x-proxy",
  "version": "0.1.0",
  "description": "Experimental JS Proxy Library to support private fields and other identity based mechanisms that will normally fails with regular Proxies",
  "module": "lib/index.js",
  "types": "types/index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc -w & http-server",
    "publish": "npm publish --access public",
    "test": "karma start karma.config.js --browsers Chrome",
    "karma": "karma start karma.config.js --single-run --browsers ChromeHeadless,FirefoxHeadless"
  },
  "files": [
    "lib/",
    "types/"
  ],
  "author": "Caridy Patiño <caridy@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "chokidar": "^3.0.1",
    "http-server": "^0.11.1",
    "istanbul-api": "^2.0.6",
    "karma": "^4.3.0",
    "karma-chrome-launcher": "^3.1.0",
    "karma-coverage": "^2.0.1",
    "karma-firefox-launcher": "^1.3.0",
    "karma-jasmine": "^2.0.1",
    "karma-rollup-preprocessor": "^7.0.2",
    "karma-safari-launcher": "^1.0.0",
    "puppeteer": "^3.1.0",
    "typescript": "^3.9.6"
  },
  "engines": {
    "node": ">=12"
  }
}
